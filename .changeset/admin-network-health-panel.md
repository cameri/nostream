---
"nostream": minor
---

feat(admin): add Network Health panel to observability dashboard

Adds GET `/admin/network-health` and a dashboard section that displays the latest NIP-66 external probe snapshot alongside live KPI metrics.
